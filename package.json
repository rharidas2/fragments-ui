{
  "name": "fragments-ui",
  "private": true,
  "version": "0.0.1",
  "description": "Fragments UI testing web app",
  "scripts": {
    "dev": "parcel serve index.html --port 1234 --host localhost",
    "build": "parcel build index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rharidas2/fragments-ui.git"
  },
  "author": "Rohith Haridas",
  "license": "UNLICENSED",
  "dependencies": {
    "oidc-client-ts": "^3.3.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "parcel": "^2.9.3",
    "vite": "^7.1.5"
  }
}
